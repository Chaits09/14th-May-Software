package utility;

import org.openqa.selenium.WebDriver;

public class BaseTest {

	public static WebDriver driver;
}
// This class is created to declared driver as a global variable and is is Reusable thats why we created it in utility(FOR PERFORING LISTENERS)